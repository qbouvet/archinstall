TODO

	chain services properly (i.e. autostarts after ssh-agent and NetworkManager, ...)

	think a bit about permissions of the different autostart / etc

	openbox + tint2

	syncthing

	pihole

	swap file + zcache

	mount data partition / do fstab / 'preserve data' setting in install.sh

	advertise_ip.sh	> source functions from bashrc in scripts



TESTING





DONE

	setup-user.sh	> git / github setup
					> ssh-agent setup

	fix autostart-boot.sh.service

	root .bashrc

	locale

	setup-system.sh	> handle user autologin
					> sudoers configuration

	advertise_ip.sh > write actual script in autostart-login.sh

	ssh root login / install as root / remove alarm during install

	wifi autoconnect> Seems to autoconnect
					> in doubt : https://wiki.archlinux.org/index.php/NetworkManager#Set_up_PolicyKit_permissions

	setup script	> split into setup-system.sh and setup-user.sh
					> autostart-login.sh - fix dbus "file not found" error on systemctl --user
					> fixed user home permissions
					> user auto-login
					> use pacman cache

	user bashrc
	bashrc files

	install script	> formatting
					> extracting
					> wifi files
					> pacman cache
